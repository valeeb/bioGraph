[pytest]
minversion = 7.0
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: fast, isolated tests of one function or class
    equivalence: comparisons between methods that should produce equivalent outcomes
    integration: tests spanning multiple modules or complete workflows
    slow: tests that are intentionally expensive
