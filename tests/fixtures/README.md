# Static fixtures

Store only tiny, human-readable inputs shared by multiple test modules here.
Large or generated datasets must not be committed; construct them in a fixture
or use the ignored project `data/` directory for local experiments.
