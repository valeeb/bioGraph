# Equivalence tests

Compare independent implementations or methods through an explicit contract.
Examples include dense versus sparse matrix paths, a method versus a small
reference implementation, or alternative methods expected to select the same
top-k genes. Prefer helpers from `tests.helpers.assertions`.
